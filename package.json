{
  "name": "article-json-selection-utils",
  "version": "1.2.0",
  "description": "Get, return & modify article-json selections",
  "main": "dist/index.js",
  "scripts": {
    "test": "babel-tape-runner test/*-test.js && semistandard | snazzy",
    "build": "rm -rf dist && mkdir -p dist && babel lib --out-dir dist",
    "watch": "rm -rf dist && mkdir -p dist && babel -w lib --out-dir dist",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/micnews/article-json-selection-utils.git"
  },
  "author": "mic.com",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/micnews/article-json-selection-utils/issues"
  },
  "homepage": "https://github.com/micnews/article-json-selection-utils#readme",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-core": "^6.10.4",
    "babel-preset-es2015": "^6.9.0",
    "babel-tape-runner": "^2.0.1",
    "semistandard": "^9.0.0",
    "snazzy": "^5.0.0",
    "tapava": "^2.2.0"
  },
  "dependencies": {
    "immutable-array-methods": "^1.3.0",
    "immutable-object-methods": "^1.1.2"
  }
}
